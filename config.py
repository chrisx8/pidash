# If the dashboard is behind a reverse proxy, enter the path here
# Include a leading slash but no trailing slashes
# LEAVE EMPTY if the path is '/' or not using a reverse proxy
URL_PREFIX = ""

# login credentials
USERNAME = "user"
PASSWORD = "change_me!"
